import React from "react";

class MenuBar extends React.Component {
  render() {
    return (
      <header role="banner">
        <div>Menu Bar</div>
        {/**
         * TODO: Add button functions
         * Save: Saves cookie as JSON string(?) Could be saved with Firebase.
         * Load: Load from firebase based on user?
         */}
      </header>
    );
  }
}

export default MenuBar;
